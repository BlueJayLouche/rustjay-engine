# Rust
target/
*.rs.bk

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo

# Build artifacts
*.o
*.so
*.dylib
*.dll

# App runtime (single-instance lock file, created in CWD). Anchored: the
# unanchored pattern also matched the crates/cuepool DIRECTORY on
# case-insensitive filesystems, silently ignoring every new source file
# under it (recorder.rs never reached CI).
/CuePool

# Node (docs)
node_modules/
dist/

# FFmpeg
ffmpeg/
